<Page  x:Class="Dental.Views.PatientCard.PatientsList"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
       xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
       xmlns:commands="clr-namespace:Dental.Infrastructures.Commands" 
       xmlns:services="clr-namespace:Dental.Services" 
       d:DesignHeight="400" d:DesignWidth="800" mc:Ignorable="d"
       dx:ThemeManager.ThemeName="Office2019White"
       xmlns:vm ="clr-namespace:Dental.ViewModels"
       xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars" xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors">

    <Page.Resources>
        <commands:PrintCommand x:Key="Print"/>
        <vm:PatientListViewModel x:Key="vm"/>
    </Page.Resources>
    
    <Border BorderBrush="LightGray" BorderThickness="1" Margin="5 5 5 0">
        <Grid DataContext="{StaticResource vm}"
                  ScrollViewer.CanContentScroll="True" 
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            
            <DockPanel LastChildFill="True" Grid.Row="0">
                <StackPanel DockPanel.Dock="Left" HorizontalAlignment="Left" Orientation="Horizontal">
                    <Image Height="25" Width="25" Margin="2 0 2 0">
                        <Image.Source>
                            <BitmapImage UriSource="pack://application:,,,/Resources/Icons/Template/people.png" />
                        </Image.Source>
                    </Image>
                    <TextBlock Text="Список пациентов" FontSize="18" Padding="0 8 5 0"/>
                </StackPanel>
                <dxb:BarContainerControl DockPanel.Dock="Top" HorizontalAlignment="Right">

                    <dxb:ToolBarControl
                        x:Name="fileToolBar" 
                        Caption="File"
                        ShowDragWidget="False"
                        AllowCustomizationMenu="False" 
                        AllowQuickCustomization="False">
                        <dxb:BarButtonItem Name="bCreate" Content="Создать карту пациента" 
                                           DataContext="{x:Static services:Navigation.Instance}"
                                           Glyph="{dx:DXImage SvgImages/Outlook Inspired/NewCustomer.svg}"
                                           LargeGlyph="{dx:DXImage SvgImages/Outlook Inspired/NewCustomer.svg}" 
                                           Command="{Binding LeftMenuClick}" 
                                           CommandParameter="Dental.Views.PatientCard.MainInfoPage"/>
                        
                        <dxb:BarButtonItem IsVisible="{Binding BtnIconList, Mode=TwoWay}" 
                                           Content="Показать список пациентов" 
                                           Glyph="{dx:DXImage SvgImages/Dashboards/MultipleMasterFilter.svg}" 
                                           LargeGlyph="{dx:DXImage SvgImages/Dashboards/MultipleMasterFilter.svg}" 
                                           Command="{Binding Path=ShowArchiveCommand}"/>
                        
                        <dxb:BarButtonItem IsVisible="{Binding BtnIconArchive, Mode=TwoWay}" 
                                           Content="Показать архивный список пациентов" 
                                           Glyph="{dx:DXImage SvgImages/Dashboards/IgnoreMasterFilters.svg}" 
                                           LargeGlyph="{dx:DXImage SvgImages/Dashboards/IgnoreMasterFilters.svg}" 
                                           Command="{Binding Path=ShowArchiveCommand}"/>
                        
                        <dxb:BarButtonItem Name="bPrint" Content="Печать"  
                                           Glyph="{dx:DXImage SvgImages/PDF Viewer/Print.svg}"
                                           LargeGlyph="{dx:DXImage SvgImages/PDF Viewer/Print.svg}" 
                                           Command="{StaticResource Print}"
                                           CommandParameter="{Binding ElementName=view}"/>
                        <dxb:BarItemLinkSeparator />
                    </dxb:ToolBarControl>
                </dxb:BarContainerControl>
            </DockPanel>

            <ScrollViewer VerticalScrollBarVisibility="Auto" VerticalAlignment="Stretch" Grid.Row="1" MaxHeight="1800">

                <dxg:GridControl  AutoGenerateColumns="None" dx:ThemeManager.ThemeName="Office2019White"
                         AutoExpandAllGroups="False"  
                         Margin="2"
                         MaxHeight="1800"
                         ItemsSource="{Binding Collection, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         AllowColumnMRUFilterList="False"
                         dx:ScrollBarExtensions.ScrollBarMode="TouchOverlap"
                         ShowBorder="True">
                    <dxg:GridControl.GroupSummary>
                        <dxg:GridSummaryItem SummaryType="Count" />
                    </dxg:GridControl.GroupSummary>
                    <dxg:GridControl.Columns>
                        <dxg:GridColumn FieldName="LastName" 
                                Header="Фамилии" 
                                GroupIndex="0" 
                                GroupInterval="Alphabetical" 
                                Width="1*"/>
                        <dxg:GridColumn FieldName="FullName"
                                Header="ФИО"                              
                                Width="2*"
                                UnboundType="String"
                                UnboundExpression="[LastName] + ' ' + [FirstName] + ' ' + [MiddleName]"
                                ReadOnly="True">
                            <dxg:GridColumn.Image >
                                <BitmapImage DecodePixelWidth="15" UriSource="pack://application:,,,/Resources/Icons/Template/User.png" />
                            </dxg:GridColumn.Image>
                            <dxg:GridColumn.CellDisplayTemplate>
                                <DataTemplate>
                                <dxe:HyperlinkEdit 
                                    Margin="0 2 0 2"
                                    Text="{Binding FullName}" 
                                    ToolTip="Перейти в анкету сотрудника"
                                    EditValue="{Binding Path=Row.FullName, Mode=OneWay}"
                                    Command="{Binding Source={StaticResource vm}, Path=OpenPatientCardCommand}"
                                    CommandParameter="{Binding Path=Row.Id, Mode=OneWay}"/>
                                </DataTemplate>
                            </dxg:GridColumn.CellDisplayTemplate>
                        </dxg:GridColumn>

                        <dxg:GridColumn FieldName="Address"  Width="3*" Header="Адрес" />
                    </dxg:GridControl.Columns>

                    <dxg:GridControl.TotalSummary>
                        <dxg:GridSummaryItem SummaryType="Count" Alignment="Right" />
                        <dxg:GridSummaryItem FieldName="FullName" SummaryType="Count" />
                    </dxg:GridControl.TotalSummary>
                    <dxg:GridControl.View>
                        <dxg:TableView 
                            ShowColumnHeaders="False"
                            ShowGroupPanel="False"
                            ShowFixedTotalSummary="True"                           
                            AllowScrollAnimation="True"
                            Name="view"
                            AllowFixedGroups="True"
                            NavigationStyle="Row"
                            AllowPrintColumnHeaderImage="True"
                            UseLegacyFilterPanel="False"
                            HighlightItemOnHover="True">
                            <dxg:TableView.InputBindings>
                                <KeyBinding Key="N"     
                                Modifiers="Ctrl"
                                Command="{Binding Path=OpenPatientCardCommand}"
                                CommandParameter="-1"  
                                    />
                                <KeyBinding Key="O"     
                                Modifiers="Ctrl"
                                Command="{Binding Path=OpenPatientCardCommand}"
                                CommandParameter="{Binding ElementName=view, Path=FocusedRow.Id}"/>
                                <KeyBinding Key="Return"     
                                Command="{Binding Path=OpenPatientCardCommand}"
                                CommandParameter="{Binding ElementName=view, Path=FocusedRow.Id}"/>
                                <MouseBinding Gesture="LeftDoubleClick"      
                                Command="{Binding Path=OpenPatientCardCommand}"
                                CommandParameter="{Binding ElementName=view, Path=FocusedRow.Id}"/>
                            </dxg:TableView.InputBindings>
                            <dxg:TableView.RowCellMenuCustomizations>
                                <dxb:BarButtonItem  GlyphSize="Small" GlyphAlignment="Left" DataContext="{StaticResource vm}"
                                            Glyph="{dx:DXImage SvgImages/Business Objects/BO_Employee.svg}" 
                                            Name="editRow" KeyGesture="Ctrl+O" Content="Открыть карту пациента"  
                                            Command="{Binding Path=OpenPatientCardCommand}"
                                            CommandParameter="{Binding ElementName=view, Path=FocusedRow.Id}"/>

                                <dxb:BarButtonItem  GlyphSize="Small" GlyphAlignment="Left" DataContext="{StaticResource vm}"
                                            Glyph="{dx:DXImage SvgImages/Business Objects/BO_Resume.svg}" 
                                            Name="addRow" KeyGesture="Ctrl+N" Content="Создать новую карту пациента"  
                                            Command="{Binding Path=OpenPatientCardCommand}"
                                            CommandParameter="-1"/>
                            </dxg:TableView.RowCellMenuCustomizations>
                        </dxg:TableView>

                    </dxg:GridControl.View>
                </dxg:GridControl>
            </ScrollViewer>
        </Grid>
    </Border>
</Page>