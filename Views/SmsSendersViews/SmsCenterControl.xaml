<UserControl
    x:Class="B6CRM.Views.SmsSendersViews.SmsCenterControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converter="clr-namespace:B6CRM.Infrastructures.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
    xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
    xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
    xmlns:local="clr-namespace:B6CRM.Views.SmsSendersViews"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
    xmlns:sysglb="clr-namespace:System.Globalization;assembly=mscorlib"
    xmlns:tree="clr-namespace:B6CRM.Infrastructures.TreeList"
    x:Name="ProstoSmsUserControl"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converter:DateToStringConverter x:Key="DateToString" />
        <converter:LongDateToStringConverter x:Key="birthToDateString" />
        <DataTemplate x:Key="ContentDetail" x:Name="dataTemplate">

            <Grid
                x:Name="templ"
                Grid.Row="0"
                MaxHeight="1200"
                Margin="2"
                VerticalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,2">

                    <dxlc:LayoutGroup
                        Margin="2"
                        VerticalAlignment="Top"
                        Header="Параметры рассылки"
                        IsCollapsed="False"
                        IsCollapsible="True"
                        Orientation="Vertical"
                        View="GroupBox">


                        <dxlc:LayoutGroup VerticalAlignment="Top" Orientation="Horizontal">

                            <dxlc:LayoutItem
                                Margin="0,0,0,2"
                                Label="Категория клиентов:"
                                LabelPosition="Top">
                                <dxe:ComboBoxEdit
                                    x:Name="clientCategoriesField"
                                    Height="27"
                                    HorizontalAlignment="Stretch"
                                    DisplayMember="Name"
                                    EditValue="{Binding ClientCategory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                                    IncrementalFiltering="True"
                                    IncrementalSearch="True"
                                    IsCaseSensitiveFilter="False"
                                    IsReadOnly="{DXBinding Expr='@e(grid).DataContext.IsReadOnly',
                                                           Mode=OneWay,
                                                           UpdateSourceTrigger=PropertyChanged}"
                                    IsTextEditable="False"
                                    ItemsSource="{Binding ElementName=grid, Path=DataContext.ClientCategories, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                    NullText="Категория клиентов"
                                    NullValueButtonPlacement="EditBox"
                                    SelectedItem="{Binding ClientCategory, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                                    ValidateOnTextInput="False">
                                    <dxe:ComboBoxEdit.StyleSettings>
                                        <dxe:RadioComboBoxStyleSettings />
                                    </dxe:ComboBoxEdit.StyleSettings>
                                </dxe:ComboBoxEdit>
                            </dxlc:LayoutItem>

                            <dxlc:LayoutItem
                                Margin="0,0,0,2"
                                Label="Канал доставки:"
                                LabelPosition="Top">
                                <dxe:ComboBoxEdit
                                    x:Name="channels"
                                    Height="27"
                                    HorizontalAlignment="Stretch"
                                    DisplayMember="Name"
                                    EditValue="{Binding Channel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                                    IncrementalFiltering="True"
                                    IncrementalSearch="True"
                                    IsCaseSensitiveFilter="False"
                                    IsReadOnly="{DXBinding Expr='@e(grid).DataContext.IsReadOnly',
                                                           Mode=OneWay,
                                                           UpdateSourceTrigger=PropertyChanged}"
                                    IsTextEditable="False"
                                    ItemsSource="{Binding ElementName=grid, Path=DataContext.Channels, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                    NullText="Каналы рассылки"
                                    NullValueButtonPlacement="EditBox"
                                    SelectedItem="{Binding Channel, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                                    ValidateOnTextInput="False">
                                    <dxe:ComboBoxEdit.StyleSettings>
                                        <dxe:RadioComboBoxStyleSettings />
                                    </dxe:ComboBoxEdit.StyleSettings>
                                </dxe:ComboBoxEdit>
                            </dxlc:LayoutItem>

                            <dx:SimpleButton
                                Height="27"
                                Margin="0,17,0,0"
                                Command="{Binding ElementName=view, Path=DataContext.LoadRecipientsCommand}"
                                CommandParameter="{Binding ElementName=templ, Path=DataContext, UpdateSourceTrigger=PropertyChanged}"
                                Content="Загрузить список клиентов"
                                Glyph="/Resources/Icons/svg/download.svg"
                                IsEnabled="{DXBinding Expr='!@e(view).DataContext.IsReadOnly',
                                                      Mode=OneWay,
                                                      UpdateSourceTrigger=PropertyChanged}" />

                            <dx:SimpleButton
                                Height="27"
                                Margin="0,17,0,0"
                                Command="{Binding ElementName=view, Path=DataContext.SendingCommand}"
                                CommandParameter="{Binding ElementName=templ, Path=DataContext, UpdateSourceTrigger=PropertyChanged}"
                                Content="Отправить рассылку"
                                Glyph="/Resources/Icons/svg/paper-plane.svg"
                                IsEnabled="{DXBinding Expr='!@e(view).DataContext.IsReadOnly and @e(clientCategoriesField).EditValue != null and @e(channels).EditValue != null',
                                                      Mode=OneWay,
                                                      UpdateSourceTrigger=PropertyChanged}" />

                            <dx:SimpleButton
                                Height="27"
                                Margin="0,17,0,0"
                                Command="{Binding ElementName=view, Path=DataContext.ResendingCommand}"
                                CommandParameter="{Binding ElementName=templ, Path=DataContext, UpdateSourceTrigger=PropertyChanged}"
                                Content="Повторить рассылку"
                                Glyph="/Resources/Icons/svg/clockwise.svg"
                                IsEnabled="{DXBinding Expr='!@e(view).DataContext.IsReadOnly and @e(clientCategoriesField).EditValue != null and @e(channels).EditValue != null',
                                                      Mode=OneWay,
                                                      UpdateSourceTrigger=PropertyChanged}" />
                        </dxlc:LayoutGroup>

                        <dxlc:LayoutItem Label="Сообщение:" LabelPosition="Top">
                            <dxe:MemoEdit
                                MinHeight="70"
                                HorizontalAlignment="Stretch"
                                VerticalContentAlignment="Top"
                                IsReadOnly="{DXBinding Expr='@e(grid).DataContext.IsReadOnly',
                                                       Mode=OneWay,
                                                       UpdateSourceTrigger=PropertyChanged}"
                                MemoTextWrapping="Wrap"
                                ShowIcon="False"
                                ShowText="True"
                                Text="{Binding Path=Msg}"
                                TextWrapping="Wrap" />
                        </dxlc:LayoutItem>

                    </dxlc:LayoutGroup>

                </DockPanel>

                <dxg:GridControl
                    x:Name="ServiceItems"
                    Grid.Row="1"
                    Width="auto"
                    MinHeight="50"
                    MaxHeight="3500"
                    VerticalAlignment="Stretch"
                    ItemsSource="{Binding Path=SmsRecipients, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                    ScrollViewer.VerticalScrollBarVisibility="Auto">

                    <dxg:GridControl.Columns>

                        <dxg:GridColumn
                            x:Name="ServiceField"
                            Width="9*"
                            MinWidth="300"
                            FieldName="Client">
                            <dxg:GridColumn.HeaderTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <svgc:SvgViewbox Height="12" Source="/Resources/Icons/svg/user1.svg" />
                                        <TextBlock Margin="3,0,0,0" Text="ФИО" />
                                    </StackPanel>
                                </DataTemplate>
                            </dxg:GridColumn.HeaderTemplate>
                            <dxg:GridColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock
                                        Margin="3,0,0,0"
                                        VerticalAlignment="Center"
                                        Text="{Binding Path=Row.Client.FullName, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
                                </DataTemplate>
                            </dxg:GridColumn.CellTemplate>
                        </dxg:GridColumn>

                        <dxg:GridColumn
                            Width="1*"
                            Header="Категория"
                            HorizontalHeaderContentAlignment="Center">
                            <dxg:GridColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        Text="{Binding Path=Row.Client.ClientCategory.Name, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                        TextAlignment="Center" />
                                </DataTemplate>
                            </dxg:GridColumn.CellTemplate>
                        </dxg:GridColumn>

                        <dxg:GridColumn
                            Width="1*"
                            Header="Контакт"
                            HorizontalHeaderContentAlignment="Center">
                            <dxg:GridColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        Text="{DXBinding Expr='@e(ProstoSmsUserControl).DataContext.GetClientContact(@e(channels).SelectedItem.Name, Row.Client)',
                                                         Mode=OneWay,
                                                         UpdateSourceTrigger=PropertyChanged}"
                                        TextAlignment="Center" />
                                </DataTemplate>
                            </dxg:GridColumn.CellTemplate>
                        </dxg:GridColumn>

                        <dxg:GridColumn
                            Width="1*"
                            DataContext="{Binding ElementName=ServiceField}"
                            Header="Статус доставки"
                            HorizontalHeaderContentAlignment="Center">
                            <dxg:GridColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        Text="{Binding Path=Row.Status, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                        TextAlignment="Center"
                                        TextWrapping="Wrap" />
                                </DataTemplate>
                            </dxg:GridColumn.CellTemplate>
                        </dxg:GridColumn>

                        <dxg:GridColumn Width="auto" AllowPrinting="False">
                            <dxg:GridColumn.CellTemplate>
                                <DataTemplate>
                                    <dxb:BarContainerControl HorizontalAlignment="Right" DockPanel.Dock="Top">
                                        <dxb:ToolBarControl
                                            AllowCustomizationMenu="False"
                                            AllowQuickCustomization="False"
                                            Background="Transparent"
                                            Caption="File"
                                            ShowDragWidget="False">

                                            <dxb:BarButtonItem
                                                Command="{Binding ElementName=view, Path=DataContext.DeleteClientFromRecipientsListCommand}"
                                                CommandParameter="{Binding RowData.Row, Mode=OneWay}"
                                                Content="Удалить"
                                                GlyphSize="Small"
                                                IsEnabled="{DXBinding Expr='!@e(view).DataContext.IsReadOnly',
                                                                      Mode=OneWay,
                                                                      UpdateSourceTrigger=PropertyChanged}">
                                                <dxb:BarButtonItem.GlyphTemplate>
                                                    <DataTemplate>
                                                        <dx:DXImage Source="/Resources/Icons/svg/times.svg" />
                                                    </DataTemplate>
                                                </dxb:BarButtonItem.GlyphTemplate>
                                            </dxb:BarButtonItem>
                                        </dxb:ToolBarControl>
                                    </dxb:BarContainerControl>
                                </DataTemplate>
                            </dxg:GridColumn.CellTemplate>
                        </dxg:GridColumn>

                    </dxg:GridControl.Columns>

                    <dxg:GridControl.View>
                        <dxg:TableView
                            x:Name="attachFileTableView"
                            AllowEditing="True"
                            AutoWidth="True"
                            NavigationStyle="Cell"
                            ShowGroupPanel="False"
                            ShowHorizontalLines="True"
                            ShowIndicator="False"
                            ShowTotalSummary="False"
                            ShowVerticalLines="False"
                            UseEvenRowBackground="True">

                            <dxg:TableView.InputBindings>
                                <KeyBinding
                                    Key="Delete"
                                    Command="{Binding ElementName=attachFileTableView, Path=DataContext.DeleteInvoiceItemCommand}"
                                    CommandParameter="{Binding ElementName=ServiceItems, Path=CurrentItem}" />

                                <KeyBinding
                                    Key="N"
                                    Command="{Binding ElementName=view, Path=DataContext.AddItemCommand}"
                                    Modifiers="Ctrl" />

                            </dxg:TableView.InputBindings>

                            <dxg:TableView.RowCellMenuCustomizations>

                                <dxb:BarButtonItem
                                    Command="{Binding ElementName=view, Path=DataContext.DeleteClientFromRecipientsListCommand}"
                                    CommandParameter="{Binding ElementName=ServiceItems, Path=CurrentItem}"
                                    Content="Удалить"
                                    Glyph="{dx:DXImage SvgImages/Icon Builder/Actions_Trash.svg}"
                                    IsEnabled="{DXBinding Expr='!@e(view).DataContext.IsReadOnly',
                                                          Mode=OneWay,
                                                          UpdateSourceTrigger=PropertyChanged}"
                                    KeyGesture="Delete" />
                            </dxg:TableView.RowCellMenuCustomizations>

                            <dxg:TableView.TotalSummaryContentStyle>
                                <Style TargetType="dx:DataContentPresenter">
                                    <Setter Property="TextBlock.FontStyle" Value="Italic" />
                                    <Setter Property="TextBlock.FontWeight" Value="DemiBold" />
                                    <Setter Property="TextBlock.TextAlignment" Value="Center" />
                                </Style>
                            </dxg:TableView.TotalSummaryContentStyle>

                        </dxg:TableView>
                    </dxg:GridControl.View>
                </dxg:GridControl>
                <dx:WaitIndicator
                    x:Name="WaitIndicator"
                    Grid.Row="1"
                    VerticalAlignment="Center"
                    Content="Загружаем рассылку ..."
                    DeferedVisibility="{Binding ElementName=UTreatmentPlan, Path=DataContext.IsWaitIndicatorVisible, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">
                    <dx:WaitIndicator.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                <TextBlock FontSize="20" Text="Ожидайте ответа" />
                                <TextBlock Text="{Binding}" />
                            </StackPanel>
                        </DataTemplate>
                    </dx:WaitIndicator.ContentTemplate>
                </dx:WaitIndicator>
            </Grid>

        </DataTemplate>
    </UserControl.Resources>


    <Border BorderBrush="LightGray" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <dxlc:LayoutGroup
                ItemLabelsAlignment="Local"
                Orientation="Horizontal"
                ScrollViewer.CanContentScroll="True"
                ScrollViewer.VerticalScrollBarVisibility="Auto">

                <dxlc:LayoutGroup
                    Name="layoutGroup1"
                    Margin="2"
                    IsCollapsed="True"
                    IsCollapsible="True"
                    ItemLabelsAlignment="Local"
                    Orientation="Vertical"
                    View="GroupBox">
                    <dxlc:LayoutGroup.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock
                                Height="22"
                                VerticalAlignment="Center"
                                Text="Настройки сервиса SmsCenter" />
                        </DataTemplate>
                    </dxlc:LayoutGroup.HeaderTemplate>

                    <dxlc:LayoutGroup DataContext="{Binding ElementName=ProstoSmsUserControl}" Orientation="Vertical">
                        <dxlc:LayoutGroup Orientation="Horizontal">
                            <dxlc:LayoutItem Label="Логин:" LabelPosition="Top">
                                <dxe:TextEdit Text="{Binding Path=DataContext.ServicePassVM.ServicePass.Login, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, ValidatesOnDataErrors=True}" />
                            </dxlc:LayoutItem>

                            <dxlc:LayoutItem Label="Пароль:" LabelPosition="Top">
                                <dxe:TextEdit Height="22" Text="{Binding Path=DataContext.ServicePassVM.ServicePass.PassDecr, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, ValidatesOnDataErrors=True}" />
                            </dxlc:LayoutItem>

                            <Button
                                Width="90"
                                Height="23"
                                Margin="0,18,0,0"
                                Command="{Binding ElementName=grid, Path=DataContext.ServicePassVM.SavePassCommand}"
                                Content="Сохранить" />

                        </dxlc:LayoutGroup>
                    </dxlc:LayoutGroup>
                </dxlc:LayoutGroup>
            </dxlc:LayoutGroup>




            <dxlc:LayoutGroup
                Grid.Row="1"
                Margin="2"
                ItemLabelsAlignment="Local"
                Orientation="Vertical"
                View="GroupBox">
                <dxlc:LayoutGroup.HeaderTemplate>
                    <DataTemplate>
                        <DockPanel HorizontalAlignment="Stretch">
                            <TextBlock
                                VerticalAlignment="Center"
                                DockPanel.Dock="Left"
                                Text="Рассылки" />

                            <dxb:BarContainerControl HorizontalAlignment="Right" DockPanel.Dock="Right">
                                <dxb:ToolBarControl
                                    AllowCustomizationMenu="False"
                                    AllowQuickCustomization="False"
                                    Background="Transparent"
                                    BarItemDisplayMode="ContentAndGlyph"
                                    Caption="File"
                                    ShowDragWidget="False">

                                    <dxb:BarButtonItem
                                        Command="{Binding ElementName=grid, Path=DataContext.EditableCommand}"
                                        Content="Закрыть для редактирования"
                                        Glyph="{dx:DXImage SvgImages/XAF/State_ItemVisibility_Show.svg}"
                                        IsVisible="{DXBinding Expr='!@e(grid).DataContext.IsReadOnly',
                                                              Mode=OneWay,
                                                              UpdateSourceTrigger=PropertyChanged}"
                                        Style="{Binding}" />

                                    <dxb:BarButtonItem
                                        Command="{Binding ElementName=grid, Path=DataContext.EditableCommand}"
                                        Content="Редактировать данные"
                                        Glyph="{dx:DXImage SvgImages/XAF/State_ItemVisibility_Hide.svg}"
                                        IsVisible="{DXBinding Expr='@e(grid).DataContext.IsReadOnly',
                                                              Mode=OneWay,
                                                              UpdateSourceTrigger=PropertyChanged}"
                                        Style="{Binding}" />

                                    <dxb:BarItemLinkSeparator />

                                    <dxb:BarButtonItem
                                        Command="{Binding ElementName=grid, Path=DataContext.AddCommand}"
                                        Content="Создать рассылку"
                                        Glyph="/Resources/Icons/svg/telegram-plane.svg"
                                        IsEnabled="{DXBinding Expr='!@e(grid).DataContext.IsReadOnly',
                                                              Mode=OneWay,
                                                              UpdateSourceTrigger=PropertyChanged}"
                                        Style="{Binding}" />

                                    <dxb:BarItemLinkSeparator />

                                    <dxb:BarButtonItem
                                        Command="{Binding ElementName=grid, Path=DataContext.SaveCommand}"
                                        Content="Сохранить"
                                        Glyph="{dx:DXImage SvgImages/Outlook Inspired/Save.svg}"
                                        IsEnabled="{DXBinding Expr='!@e(grid).DataContext.IsReadOnly',
                                                              Mode=OneWay,
                                                              UpdateSourceTrigger=PropertyChanged}"
                                        Style="{Binding}" />

                                    <dxb:BarItemLinkSeparator />

                                    <dxb:BarButtonItem
                                        Command="{Binding ElementName=ProstoSmsUserControl, Path=DataContext.SetFilterCommand}"
                                        CommandParameter="2"
                                        Content="Все рассылки"
                                        Glyph="{dx:DXImage SvgImages/Dashboards/MasterFilter.svg}"
                                        GlyphSize="Small"
                                        IsVisible="{DXBinding Expr='@e(ProstoSmsUserControl).DataContext.IsShowSmsSenders == null',
                                                              Mode=OneWay,
                                                              UpdateSourceTrigger=PropertyChanged}" />

                                    <dxb:BarButtonItem
                                        Command="{Binding ElementName=ProstoSmsUserControl, Path=DataContext.SetFilterCommand}"
                                        CommandParameter="1"
                                        Content="Отправленные"
                                        Glyph="{dx:DXImage SvgImages/Dashboards/MultipleMasterFilter.svg}"
                                        GlyphSize="Small"
                                        IsVisible="{DXBinding Expr='@e(ProstoSmsUserControl).DataContext.IsShowSmsSenders == 2',
                                                              Mode=OneWay,
                                                              UpdateSourceTrigger=PropertyChanged}" />

                                    <dxb:BarButtonItem
                                        Command="{Binding ElementName=ProstoSmsUserControl, Path=DataContext.SetFilterCommand}"
                                        Content="Не отправленные"
                                        Glyph="{dx:DXImage SvgImages/Dashboards/IgnoreMasterFilters.svg}"
                                        GlyphSize="Small"
                                        IsVisible="{DXBinding Expr='@e(ProstoSmsUserControl).DataContext.IsShowSmsSenders == 1',
                                                              Mode=OneWay,
                                                              UpdateSourceTrigger=PropertyChanged}" />

                                </dxb:ToolBarControl>
                            </dxb:BarContainerControl>
                        </DockPanel>
                    </DataTemplate>
                </dxlc:LayoutGroup.HeaderTemplate>

                <dxg:GridControl
                    Name="grid"
                    Grid.Row="1"
                    MaxHeight="2000"
                    Margin="2"
                    VerticalAlignment="Stretch"
                    AutoGenerateColumns="None"
                    ItemsSource="{Binding Sms, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">
                    <dxg:GridControl.View>
                        <dxg:TableView
                            Name="view"
                            AllowPrintColumnHeaderImage="True"
                            AllowScrollToFocusedRow="True"
                            AutoWidth="True"
                            BestFitModeOnSourceChange="VisibleRows"
                            NavigationStyle="Cell"
                            PrintAutoWidth="True"
                            SearchPanelAllowFilter="False"
                            ShowGroupPanel="False"
                            ShowSearchPanelMode="Never"
                            UseAnimationWhenExpanding="True"
                            UseLegacyFilterPanel="True"
                            VerticalScrollbarVisibility="Auto">


                            <dxg:TableView.RowCellMenuCustomizations>

                                <dxb:BarButtonItem
                                    Command="{Binding ElementName=grid, Path=DataContext.AddCommand}"
                                    CommandParameter="{Binding ElementName=UTreatmentPlan, Path=DataContext.Model, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                    Content="Создать рассылку"
                                    Glyph="/Resources/Icons/svg/telegram-plane.svg"
                                    IsEnabled="{DXBinding Expr='!@e(grid).DataContext.IsReadOnly',
                                                          Mode=OneWay,
                                                          UpdateSourceTrigger=PropertyChanged}"
                                    KeyGesture="Ctrl+N" />

                                <dxb:BarButtonItem
                                    Command="{Binding ElementName=grid, Path=DataContext.DeleteCommand}"
                                    CommandParameter="{Binding ElementName=grid, Path=CurrentItem}"
                                    Content="Удалить рассылку"
                                    Glyph="{dx:DXImage SvgImages/Icon Builder/Actions_Trash.svg}"
                                    GlyphAlignment="Left"
                                    GlyphSize="Small"
                                    IsEnabled="{DXBinding Expr='!@e(grid).DataContext.IsReadOnly',
                                                          Mode=OneWay,
                                                          UpdateSourceTrigger=PropertyChanged}"
                                    KeyGesture="Delete" />

                            </dxg:TableView.RowCellMenuCustomizations>
                        </dxg:TableView>
                    </dxg:GridControl.View>

                    <dxg:GridColumn
                        Width="3*"
                        MinWidth="80"
                        FieldName="Name"
                        Header="Название рассылки">
                        <dxg:GridColumn.HeaderTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <svgc:SvgViewbox Height="9" Source="/Resources/Icons/svg/list.svg" />
                                    <TextBlock Margin="3,0,0,0" Text="Название рассылки" />
                                </StackPanel>
                            </DataTemplate>
                        </dxg:GridColumn.HeaderTemplate>
                        <dxg:GridColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock
                                    Margin="0,3,0,0"
                                    Text="{Binding Row.Name, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                    TextWrapping="Wrap" />
                            </DataTemplate>
                        </dxg:GridColumn.CellTemplate>
                        <dxg:GridColumn.CellEditTemplate>
                            <DataTemplate>
                                <dxe:TextEdit
                                    Margin="0,3,0,0"
                                    HorizontalAlignment="Stretch"
                                    EditValue="{Binding Row.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    TextWrapping="Wrap" />
                            </DataTemplate>
                        </dxg:GridColumn.CellEditTemplate>
                    </dxg:GridColumn>

                    <dxg:GridColumn
                        Width="1*"
                        FieldName="Date"
                        Header="Дата"
                        HorizontalHeaderContentAlignment="Center">
                        <dxg:GridColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock
                                    Margin="0,3,0,0"
                                    HorizontalAlignment="Center"
                                    Text="{Binding Row.Date, StringFormat='{}{0:dd MMMM yyyy HH:mm}', ConverterCulture=ru-RU, Converter={StaticResource DateToString}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                    TextWrapping="Wrap" />
                            </DataTemplate>
                        </dxg:GridColumn.CellTemplate>
                        <dxg:GridColumn.CellEditTemplate>
                            <DataTemplate>
                                <dxe:DateEdit
                                    x:Name="DateEdit"
                                    Margin="0,0,8,0"
                                    HorizontalAlignment="Stretch"
                                    DisplayFormatString="D"
                                    EditValue="{Binding Row.Date, Mode=TwoWay, StringFormat=D, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource birthToDateString}, ValidatesOnDataErrors=True}"
                                    IsReadOnly="{DXBinding Expr='@e(grid).DataContext.IsReadOnly',
                                                           Mode=OneWay,
                                                           UpdateSourceTrigger=PropertyChanged}"
                                    Mask="f"
                                    MaskType="DateTime">
                                    <dxe:DateEdit.StyleSettings>
                                        <dxe:DateEditNavigatorWithTimePickerStyleSettings />
                                    </dxe:DateEdit.StyleSettings>
                                </dxe:DateEdit>
                            </DataTemplate>
                        </dxg:GridColumn.CellEditTemplate>
                    </dxg:GridColumn>

                    <dxg:GridColumn
                        Width="1*"
                        FieldName="SendingStatus"
                        Header="Отправлено"
                        HorizontalHeaderContentAlignment="Center">
                        <dxg:GridColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock
                                    Margin="0,3,0,0"
                                    HorizontalAlignment="Center"
                                    Text="{Binding Row.SendingStatus.Name, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                    TextWrapping="Wrap" />
                            </DataTemplate>
                        </dxg:GridColumn.CellTemplate>
                        <dxg:GridColumn.CellEditTemplate>
                            <DataTemplate>
                                <dxe:ComboBoxEdit
                                    HorizontalAlignment="Stretch"
                                    DisplayMember="Name"
                                    EditValue="{Binding Row.SendingStatus, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                                    IncrementalFiltering="True"
                                    IncrementalSearch="True"
                                    IsCaseSensitiveFilter="False"
                                    IsReadOnly="{DXBinding Expr='@e(grid).DataContext.IsReadOnly',
                                                           Mode=OneWay,
                                                           UpdateSourceTrigger=PropertyChanged}"
                                    IsTextEditable="False"
                                    ItemsSource="{Binding ElementName=grid, Path=DataContext.SendingStatuses, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                    NullText="Статус"
                                    NullValueButtonPlacement="EditBox"
                                    SelectedItem="{Binding Row.SendingStatus, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                                    ValidateOnTextInput="False">
                                    <dxe:ComboBoxEdit.StyleSettings>
                                        <dxe:RadioComboBoxStyleSettings />
                                    </dxe:ComboBoxEdit.StyleSettings>
                                </dxe:ComboBoxEdit>
                            </DataTemplate>
                        </dxg:GridColumn.CellEditTemplate>
                    </dxg:GridColumn>

                    <dxg:GridColumn Width="20">
                        <dxg:GridColumn.CellTemplate>
                            <DataTemplate>
                                <dxb:BarContainerControl
                                    Margin="0,0,5,0"
                                    HorizontalAlignment="Right"
                                    DockPanel.Dock="Top">
                                    <dxb:ToolBarControl
                                        x:Name="fileToolBar"
                                        AllowCustomizationMenu="False"
                                        AllowQuickCustomization="False"
                                        Background="Transparent"
                                        Caption="File"
                                        ShowDragWidget="False">

                                        <dxb:BarButtonItem
                                            Command="{Binding ElementName=view, Path=DataContext.DeleteCommand}"
                                            CommandParameter="{Binding Row}"
                                            Content="Удалить рассылку"
                                            Glyph="{dx:DXImage SvgImages/Icon Builder/Actions_Trash.svg}"
                                            GlyphSize="Small"
                                            IsEnabled="{DXBinding Expr='!@e(view).DataContext.IsReadOnly',
                                                                  Mode=OneWay,
                                                                  UpdateSourceTrigger=PropertyChanged}" />
                                        <dxb:BarItemLinkSeparator />
                                    </dxb:ToolBarControl>
                                </dxb:BarContainerControl>
                            </DataTemplate>
                        </dxg:GridColumn.CellTemplate>
                    </dxg:GridColumn>

                    <dxg:GridControl.DetailDescriptor>
                        <dxg:ContentDetailDescriptor ContentTemplate="{StaticResource ContentDetail}" HeaderContent="ContentDetailDescriptor" />
                    </dxg:GridControl.DetailDescriptor>
                </dxg:GridControl>

            </dxlc:LayoutGroup>
        </Grid>
    </Border>
</UserControl>
